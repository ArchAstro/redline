---
"@archastro/redline": patch
---

Accept in-page hash changes when checking that a content-script sender still matches its tab. Clicking a `#section` link no longer makes Submit fail with "Page sender was rejected."
