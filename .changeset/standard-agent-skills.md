---
"@archastro/redline": minor
---

Replace the duplicated Claude and Codex plugins with one portable agent skill that users install, update, and remove directly through the standard `skills` CLI. Redline's CLI no longer manages agent skill or plugin state.
